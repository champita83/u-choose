services:
  - type: web
    name: alex-chatbot-backend
    env: python
    buildCommand: ""
    startCommand: gunicorn main:app
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
